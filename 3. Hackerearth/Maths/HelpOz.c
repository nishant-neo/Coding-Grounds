#include <stdio.h>

int main()
{
    int n, i;
    scanf("%d", &n);
    int arr[n+1];
    for( i = 1; i <= n; i ++)
        scanf("%d",&a[i]);
    for( i = 1; i <= n ; i++)
    {
        for()
    }
    return 0;
}
